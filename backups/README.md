# ChemZim Project Backups Directory

This directory stores backup snapshots of curriculum content, question banks, and configuration files.

## Backup Standards

1. Store backup files with date suffixes (e.g., `curriculum-backup-2026-07-30.json`).
2. Never commit sensitive credentials or environment variables into backup files.
